一，基础配置
	SQLALCHEMY_DATABASE_URI   数据库连接地址

	SQLALCHEMY_BINDS          用于绑定多个数据库地址，例如SQLALCHEMY_BINDS = {
	    'users':        'mysqldb://localhost/users',
	    'appmeta':      'sqlite:////path/to/appmeta.db'
	}

	SQLALCHEMY_ECHO          true输出sql语句,方便调试y用

	SQLALCHEMY_POOL_SIZE    设置数据库连接池大小,默认为5

	SQLALCHEMY_POOL_TIMEOUT   数据库连接超时时间,默认为10s

	SQLALCHEMY_POOL_RECYCLE  自动回收连接的秒数

	SQLALCHEMY_MAX_OVERFLOW  控制在连接池达到最大值后可以创建的连接数。当这些额外的 连接回收到连接池后将会被断开和抛弃。

	

